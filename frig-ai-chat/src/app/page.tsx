import ChatInterface from './components/ChatInterface'; // Import the new component

export default function Home() {
  return (
    // The ChatInterface component will fill the main content area defined in layout.tsx
    <ChatInterface />
  );
}
